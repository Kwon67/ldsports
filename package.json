{
  "name": "ldsports",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend-next & npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend-next",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend-next",
    "lint": "npm run lint --prefix frontend-next && npm run lint --prefix backend",
    "format": "npm run format --prefix frontend-next && npm run format --prefix backend",
    "format:check": "npm run format:check --prefix frontend-next && npm run format:check --prefix backend",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3"
  },
  "lint-staged": {
    "frontend-next/**/*.{js,jsx,ts,tsx}": [
      "npm run lint --prefix frontend-next -- --fix",
      "npm run format --prefix frontend-next"
    ],
    "backend/**/*.js": [
      "npm run lint --prefix backend -- --fix",
      "npm run format --prefix backend"
    ]
  }
}
